import fs from "fs";

//* fs.Promise Create a Promise
const fsPromise = fs.promises;
/*
    fs is the Object which have a property named promises which allows us to
    read and write file asynchronously withOut Using Callbacks
*/