/*
    Models : In world of mongoose models and schema is different Types. Schema is
        the blueprint or structure of the document in the collection where models
        is the constructor compiled from schema definations. We can understand like
        models is set of APIs and Instance an Instance of Model is called as Document.
        Using Models we give the structure to our collection but using models we can
        perform data related operations CRUD.
*/