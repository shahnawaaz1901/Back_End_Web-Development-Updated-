/*
    Cookie-Parser : Cookie Parser Parse Cookie header and Populate req.cookie
                with an object keyed by the cookie names. Optionally You May
                enable signed cookie support by parsing a secret string which
                assigns req.secret so it may be used by other middlewares.
*/